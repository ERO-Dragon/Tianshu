package com.rheinmetal.tianshu.protocol;

public enum BrokerType {
    EXCLUSIVE_INTERRUPT,
    PARALLEL_LIMIT,
    LATEST_ONLY,
    BOUNDED_QUEUE,
    STATELESS_FAST_PATH,
    MAIN_THREAD,
    SERVER_PACKET
}

